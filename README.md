# Expense Anomaly Agent

An AI agent that ingests expense/invoice records, checks them against policy
rules and historical patterns, and either auto-approves, flags for human
review, or asks a clarifying question — with a self-verification step and
hard-coded guardrails on top.

Built as a take-home project for a Senior Applied AI Engineer interview.

## Status

- [x] Backend + frontend scaffolding (Express/TS, Vite/React/TS)
- [x] Synthetic dataset: 40 expense records + vendor/employee/policy reference data
- [x] Ground-truth evaluation labels for all 40 records
- [x] Deterministic tools: policy check, vendor history, employee spending pattern, duplicate detection
- [x] Agent core loop wired to the Claude API (tool use, multi-round reasoning)
- [x] Self-verification step — hard-coded guardrails re-check the model's own conclusion
- [x] Action enforcement — flag/clarify decisions are confirmed against real store state, not just trusted from the model's claim
- [x] Evaluation harness — runs the full dataset against ground truth, reports false-approve rate, false-flag rate, clarify recall
- [x] Demo UI — per-row and "run all" live decision feed, color-coded by outcome, expandable reasoning

## Structure

```
backend/
  src/
    index.ts              Express app entry — API routes
    data/
      expenses.json        40 synthetic expense records
      vendors.json          18 vendors with transaction history
      employees.json        5 employees with per-category spending baselines
      policy.json            Deterministic policy thresholds (limits, ceilings)
    tools/
      checkPolicyRules.ts        Deterministic — policy limits, receipts, hard ceiling
      getVendorHistory.ts        Deterministic — first-time vendor, amount deviation
      getEmployeeSpendingPattern.ts  Deterministic — baseline comparison
      checkDuplicate.ts          Deterministic — possible-duplicate detection
      flagForHumanReview.ts      Action — writes to the review queue
      requestClarification.ts    Action — writes to the clarification queue
      schemas.ts                 Anthropic tool-use schemas for all six tools
      manual-test.ts             Ad-hoc sanity check against the tricky records
    agent/
      types.ts               Shared types: Expense, AgentDecision, tool result shapes
      dataStore.ts             Loads and types the JSON reference data
      store.ts                  In-memory review queue + clarification queue
      executeTool.ts             Dispatches a model tool_use call to the real function
      systemPrompt.ts             The agent's instructions, decision categories, guardrails
      loop.ts                     Core perceive → reason → act loop + self-verification
      run-single.ts                 CLI runner: evaluate one expense by ID
    eval/
      ground-truth.json      Expected action + reasoning for each of the 40 records
                              (kept separate from data/ so the agent never sees it)
      run-eval.ts             Runs the full dataset, computes accuracy metrics
      eval-results.json      Generated by run-eval.ts — full per-record results
frontend/
  src/
    App.tsx                 Expense table with live per-row / run-all agent evaluation
    App.css
generate_dataset.py     random dataset generator for users to create their own set of data
```

## Dataset

`expenses.json` has 40 records: mostly straightforward cases (so the eval
isn't gamed by an agent that just flags everything) plus a dozen
deliberately tricky edge cases — borderline policy thresholds, first-time
vendors at different amounts, a possible duplicate, missing receipts
above/below the receipt-required threshold, a suspiciously round number, a
hard-ceiling breach, a category/description mismatch, and a missing
required field.

`ground-truth.json` in `eval/` has the expected action (`approve` / `flag`
/ `clarify`) and reasoning for each record. One label (`EXP004`) was
corrected mid-build after the eval harness itself surfaced a genuine
authoring mistake — a $95 meal mislabeled "approve" that actually exceeds
the $75 meals policy limit. The agent's own policy check caught it before
a human review did.

## How the agent works

1. **Perceive** — the agent receives one expense record.
2. **Reason** — it calls whichever of the four read-only tools are
   relevant (policy check, vendor history, employee pattern, duplicate
   check) and reads the free-text description itself for signals no tool
   can catch (e.g. a personal purchase misfiled under a business category).
3. **Act** — it concludes `approve`, `flag`, or `clarify`. A `flag` or
   `clarify` conclusion requires it to have actually called the matching
   action tool during reasoning.
4. **Verify** — before the decision is returned, code-level guardrails
   re-check it independently of the model's own reasoning:
   - Hard ceiling: never let "approve" through above the configured
     dollar ceiling, regardless of what the model concluded.
   - Policy disagreement: if the deterministic policy check found a
     violation but the model said "approve" anyway, downgrade to "flag".
   - Action enforcement: if the model claimed "flag" or "clarify" but
     never actually called the corresponding tool, the code calls it
     instead of trusting the claim.

This means the model's own judgment is never the last word — every
decision either matches a guardrail-verified conclusion or gets
overridden with a clear, logged reason.

## API

- `GET /api/health` — liveness check
- `GET /api/expenses` — all 40 expense records, with vendor and employee names joined in
- `POST /api/evaluate/:id` — runs the full agent loop on one expense, returns its decision

## Running locally

```bash
# Terminal 1
cd backend
npm install
cp .env.example .env   # add your ANTHROPIC_API_KEY
npm run dev

# Terminal 2
cd frontend
npm install
npm run dev
```

Backend: http://localhost:3001
Frontend: http://localhost:5173 (dev server proxies `/api/*` to the backend)

Open the frontend in a browser. Click **Evaluate** on any row to run the
agent on that expense, or **Run Agent on All** to evaluate all 40
sequentially and watch decisions land one at a time. Click a decision
badge to expand the model's reasoning and risk factors.

## Testing without the UI

```bash
cd backend

# Sanity-check the deterministic tools against the tricky records
npx tsx src/tools/manual-test.ts

# Run the agent on a single expense from the terminal
npx tsx src/agent/run-single.ts EXP038

# Run the full eval harness (costs real API calls — try --limit first)
npx tsx src/eval/run-eval.ts --limit 5
npx tsx src/eval/run-eval.ts
```

## Future improvements

This is a scoped take-home demo, not a production system. The honest gap
isn't the agent logic — it's everything currently in memory or on disk
that a real deployment would need to be durable, observable, and safe to
run unattended. Roughly in the order I'd actually tackle them:

### Would block production, not just polish

- **Persistent storage instead of in-memory queues.** `reviewQueue` and
  `clarificationRequests` (in `agent/store.ts`) vanish on server restart.
  Needs Postgres or similar so a flagged expense survives a deploy, and
  so multiple server instances share state instead of each holding half of it.
- **Idempotency / exactly-once processing.** Nothing stops the same
  expense from being evaluated twice and double-queued. Needs a
  processing-state field per record with a DB-level lock or unique
  constraint, not an in-memory array.
- **Auth on the API.** `POST /api/evaluate/:id` has zero access control —
  anyone who can reach the server can trigger a paid LLM call on
  arbitrary IDs. Needs at minimum an API key or session check.
- **Retry / timeout handling around the Claude API call.** A network
  blip or slow response currently fails the whole record. Needs
  exponential backoff on retryable errors and a hard timeout so one slow
  call can't hang a batch run.

### Needed before trusting it at real volume

- **Real database for vendors/employees/policy**, not JSON files. Fine
  at 40 records; falls over once vendor history needs to update in real
  time. Also what makes `checkDuplicate` actually correct at scale —
  right now it linearly scans the full in-memory expense array.
- **Structured logging and tracing per decision** — which tools were
  called, in what order, with what results, how long the loop took. Both
  for debugging and for the audit trail a real finance team would require.
- **Continuous monitoring on the false-approve rate specifically**, not
  just a manually-run eval script. That's the one metric where a silent
  regression (a prompt change, a model version bump) has real financial
  consequence.
- **Prompt/eval versioning in CI.** The system prompt and the eval set
  currently have no formal link. A real setup runs the eval suite
  automatically against every prompt change before it ships — the eval
  harness as a regression test, not a one-off report.

### Real, but not urgent

- **Containerization (Docker) and orchestration (Kubernetes or a managed
  equivalent).** Mechanical once the app is stateless — the actual
  blocker is the in-memory store above, not the containerization itself.
- **Public hosting / CI-CD pipeline.** Same dependency — worth doing
  once there's a real backing store and auth, not before; hosting an
  unauthenticated LLM-calling endpoint publicly is a cost and abuse risk.
- **Human review queue UI.** `flagForHumanReview` writes to an array with
  no way for an actual auditor to see or act on it — needs a second UI
  surface entirely, not just the submitter-facing table this demo has.
- **Notification delivery for clarification requests.**
  `requestClarification` currently just logs a question; a real system
  needs to get that question in front of the submitter (email, Slack,
  in-app) and handle the async reply.
- **Cost/latency optimization.** Batching multiple expenses per API call,
  caching tool results within a session, and evaluating whether a
  cheaper model handles the easy majority-approve cases while reserving
  the current model for anything a first pass already flags as ambiguous.

### Explicitly out of scope, and why

- **Vector DB / semantic search** — nothing in this domain benefits from
  it; the four tools need structured exact-match lookups, not semantic
  retrieval.
- **Multi-agent orchestration** — one agent with one clear decision
  boundary is the right shape for this problem. Splitting it into
  cooperating agents would add coordination complexity without solving
  anything the current single loop can't already do.